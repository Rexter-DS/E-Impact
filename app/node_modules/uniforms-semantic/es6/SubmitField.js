import { __rest } from "tslib";
import classnames from 'classnames';
import React from 'react';
import { filterDOMProps, useForm } from 'uniforms';
export default function SubmitField(_a) {
    var { className, disabled, inputRef, value } = _a, props = __rest(_a, ["className", "disabled", "inputRef", "value"]);
    const { error, state } = useForm();
    return (React.createElement("input", Object.assign({ className: classnames('ui', className, 'button'), disabled: disabled === undefined ? !!(error || state.disabled) : disabled, ref: inputRef, type: "submit" }, (value ? { value } : {}), filterDOMProps(props))));
}
